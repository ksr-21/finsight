import React from 'react';

/**
 * @deprecated This component is no longer used. API key management is handled securely via
 * environment variables (`process.env.API_KEY`) and is not exposed to the client-side UI.
 * This aligns with security best practices and simplifies the user experience.
 */
const ApiKeyModal: React.FC = () => null;

export default ApiKeyModal;